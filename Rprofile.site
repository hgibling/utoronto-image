local({
    r <- getOption("repos")
    options(BioC_mirror = "https://packagemanager.rstudio.com/bioconductor")
    # Picked from https://packagemanager.rstudio.com/client/#/repos/4/overview for bioconductor 3.12, R4.0
    r["CRAN"] <- "https://packagemanager.rstudio.com/cran/__linux__/focal/2021-05-20"
    options(repos = r)
    options(HTTPUserAgent = sprintf("R/%s R (%s)", getRversion(), paste(getRversion(), R.version$platform, R.version$arch, R.version$os)))
})